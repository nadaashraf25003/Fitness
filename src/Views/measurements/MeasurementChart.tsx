import React from 'react';
import { LineChart } from '../../Components/charts/LineChart';

export const MeasurementChart: React.FC = () => {
  return <LineChart data={[]} title="Progress Metric Trend" />;
};
